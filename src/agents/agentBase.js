import { generateJson } from '../llm/llmClient.js';
export const persona={technicalAgent:'Technical Agent',hrAgent:'HR & Culture Agent',skepticAgent:'Skeptic Agent',hiringManagerAgent:'Hiring Manager Agent'};
export function buildPrompt(name,instructions,profile){return `You are ${persona[name]}. ROUND ONE IS ISOLATED. You receive ONLY source documents below; do not assume other agents' views. Job Description is the benchmark. Never invent evidence. Every score/claim MUST include verbatim quote evidence from RESUME, TRANSCRIPT, or JD. If evidence is absent say exactly "Insufficient evidence in source documents". Return JSON: {agent,score:0-100,confidence:0-100,weight:0-1,summary,evidence:[{claim,source,quote}],flags:[{severity:low|medium|high|critical,claim,evidence:{source,quote}}],stance}.\nINSTRUCTIONS:${instructions}\nRESUME:\n${profile.resumeText}\nTRANSCRIPT:\n${profile.transcriptText}\nJOB DESCRIPTION:\n${profile.jdText}`}
export async function runAgent(name,instructions,profile){return generateJson(buildPrompt(name,instructions,profile));}
export function fallbackAgent(name,error){return {agent:persona[name],score:null,confidence:0,weight:0,summary:'Agent analysis was unavailable; no candidate conclusion was inferred.',evidence:[],flags:[{severity:'high',claim:'Agent evaluation unavailable',evidence:{source:'system',quote:'Agent request failed. No candidate evidence was fabricated.'}}],stance:'Insufficient evidence in source documents',error:{code:error.code||'AGENT_FAILURE',message:error.message}}}
